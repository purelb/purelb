---
apiVersion: v1
kind: Service
metadata:
  name: nginx-remote-dualstack
  namespace: test
  labels:
    test-suite: remote
  annotations:
    purelb.io/service-group: remote
spec:
  type: LoadBalancer
  ipFamilyPolicy: RequireDualStack
  ipFamilies:
  - IPv4
  - IPv6
  selector:
    app: nginx
  ports:
  - port: 80
    targetPort: 80
