---
apiVersion: v1
kind: Service
metadata:
  name: nginx
  annotations:
    purelb.io/service-group: default
spec:
  type: LoadBalancer
  ipFamilyPolicy: PreferDualStack
  ipFamilies:
    - IPv4
    - IPv6
  selector:
    app: nginx
  ports:
    - name: http
      port: 80
      targetPort: 80
