apiVersion: v1
kind: Service
metadata:
  name: {{ include "purelb.fullname" . }}
  labels:
    {{- include "purelb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "purelb.selectorLabels" . | nindent 4 }}
