apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "purelb.fullname" . }}-test-connection"
  labels:
    {{- include "purelb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "purelb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
